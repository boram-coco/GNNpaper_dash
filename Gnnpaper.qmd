---
title: "Graph representation corresponds to the credit card number"
author: "Boram_coco"
format: 
    dashboard:
     theme: default
     nav-buttons:
      - icon: github
        href: https://github.com/boram-coco
execute: 
  enabled: true
  cache: false
  freeze: false
  error: true
---

```{python}
import pandas as pd
import dash
from dash import dcc, html
from dash.dependencies import Input, Output
import plotly.express as px
import networkx as nx
import torch_geometric

# data
df50 = pd.read_csv('~/Dropbox/GNNpaper_dash/data/df50_fraud.csv')

```


```{python}
#| title: cc_num별 거래 자료
import itables
import itables.options as opt
result_df = df50.groupby("cc_num")["is_fraud"].agg(거래수='count', 사기거래수='sum').reset_index()
itables.show(result_df)
```


